CREATE TABLE IF NOT EXISTS public.dialogues(
                                             "from" int NOT NULL,
                                             "to" int NOT NULL,
                                             text text NOT NULL
);